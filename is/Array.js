/**
* Checks a value to see if it is an array
* @function
*   @param {any} v The value to be checked
*   @returns {boolean}
* @naming
*   @alias is_array
*/
function isArray(v) {
    return Array.isArray(v);
}