/**
* @test
*   @title PunyJS.core.net.http._WebSocketClient: browser, defaults
*/
function webSocketClientTest1(
    controller
    , mock_callback
) {


    arrange(
        async function arrangeFn() {

        }
    );

    act(
        function actFn() {

        }
    );

    assert(
        function assertFn(test) {


        }
    );
}