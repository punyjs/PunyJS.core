{
    "name": "PunyJS.core"
    , "description": "A library of base root scoped functions and several helper"

    , "entries": [

        {
            "type": "javascript"
            , "output": "builds/{name}/app.js"
        }

        , {
            "type": "test"
            , "output": "builds/{name}"
        }

    ]
}